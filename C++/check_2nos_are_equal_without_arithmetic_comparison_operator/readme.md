## Check if two numbers are equal without using arithmetic or comparison operators

We know, for two equal numbers XOR operator returns 0. Hence, I have used *Bitwise XOR operator (^)* to solve this problem. 